**Avis : ne pas appliquer le lot tel quel.** La migration `20260801002350`, exécutée après les migrations déjà présentes en PREPROD, régresse le défaut `live_data_source`. La migration `20260807000400` est sans effet métier sur le snapshot observé, mais son algorithme n'est pas une réparation universellement sûre. La dernière migration améliore les ACL sans corriger une faille préexistante de la RPC de création d'association.

Audit du commit `9c9716de870b7b5f6035283f79416cc4459f5b4d`, PREPROD `qaguotdproxamgudnnsd`, PostgreSQL 17.6. Dernier snapshot : **4 septembre 2026, 19:37:28 UTC**. Connexion ciblée par `HSP_PREPROD_DATABASE_URL`, transaction `REPEATABLE READ READ ONLY`, `transaction_read_only=on`, clôturée par rollback. Aucun DDL, DML, appel de RPC de mutation, seed, migration, repair ou test avec écritures exécuté. Les fichiers SQL des migrations restent inchangés.

Les [preuves JSON](2026-09-04-preprod-evidence.json) contiennent les catalogues distants, résultats agrégés, historique et SHA-256 des quatre fichiers. Le [préflight SQL](2026-09-04-preprod-preflight.sql) reproduit les lectures; vérifier explicitement le projet de connexion avant toute exécution. Les prédictions ci-dessous proviennent du code et du snapshot, pas d'une répétition des migrations. Les tests SQL existants ont été lus, pas exécutés.

L'historique distant comparé à tous les fichiers locaux confirme **exactement les quatre versions manquantes**, sans version distante absente du dépôt. Cela confirme le périmètre du dry-run, mais ne prouve ni l'exécution SQL future ni l'identité du contenu de toutes les migrations historiques.

| Migration | Impact actuel prédit | Idempotence | Avis |
| --- | --- | --- | --- |
| `20260801002350` | 7 setups réécrits; 0 backfill; défaut `announcer` → `scribe`; ACL RPC resserrées | Structure rejouable sous préconditions; pas strictement idempotente sur données/effets | Bloquée en l'état par la régression du défaut |
| `20260807000400` | 0 modèle réordonné, 0 classe, 0 organisme, 0 exigence créés | Stable à données fixes, après succès complet et sans concurrence | Faible risque sur ce snapshot vide de modèles; recompter avant application |
| `20260824130000` | 1 warmup inchangé au déploiement; nouvelles possibilités de lecture et RPC live | Définition/policy rejouables si signature compatible | Validation fonctionnelle et RLS requise |
| `20260824131000` | Ajout de `is_test_mode=false` aux 4 organisations; 6 policies; ACL RPC | Convergente si colonne/signature compatibles | Faille RPC préexistante à traiter avant ouverture fonctionnelle |

**1. `20260801002350_reconcile_showscore_live_setup_compatibility.sql`**

Toutes les instructions ont été examinées : suppression conditionnelle du trigger et de sa fonction de marquage; ajout conditionnel de sept colonnes; conversion de `live_source_changed_by` en texte; UPDATE global des setups; defaults/NOT NULL; remplacement de huit noms possibles de contraintes par quatre CHECK; backfill ciblé des approbations juge; suppression/recréation de deux RPC; REVOKE/GRANT; notification PostgREST.

En PREPROD, les sept colonnes existent déjà; `live_source_changed_by` est déjà `text`; les quatre CHECK canoniques existent. Le trigger/fonction `stamp_show_score_live_source_change` est absent. Les recherches de dépendances cataloguées pour les fonctions supprimées et cette colonne sont vides. Aucune valeur des sept setups ne viole les nouveaux CHECK et aucune des quatre valeurs à compléter n'est NULL. Les sessions juge sont vides. Le PK sur `block_id` supporte les UPSERT des RPC.

Le défaut distant est **`'announcer'::text`**, établi par `20260805000600_showscore_setup_defaults.sql`, déjà appliquée. La réconciliation manquante remet explicitement **`'scribe'`**, et la migration de defaults ne sera pas rejouée automatiquement. Les six setups actuellement `announcer` conservent leur valeur; ce sont les futurs INSERT omettant la source qui changent de comportement. Le défaut `qualified_rider_count=6` demeure intact. Une base reconstruite dans l'ordre chronologique et PREPROD rattrapant cette migration ancienne aboutiraient donc à des defaults différents.

L'UPDATE sans WHERE écrit **les 7 lignes**, même si COALESCE ne change rien. Les triggers distants confirment :

- `touch_updated_at` modifie leurs dates; l'opération produit des écritures/WAL.
- `set_show_score_block_setup_refs` recopie organisation, show, journée, pattern et custom_pattern depuis le bloc. Aucune différence actuelle, mais ce trigger pourrait écraser un contexte divergent dans un autre snapshot.
- `sync_show_score_announcer_session_from_setup` est déclenché parce que `live_data_source` figure dans SET, même si sa valeur reste identique. Il peut créer une session manquante ou remplacer les runs d'une session non commencée/non terminée et incrémenter sa révision. Ici : **6 setups annonceur, 6 sessions, 0 insertion et 0 mise à jour prédites**.
- Le trigger de broadcast est présent : **7 setups éligibles**, donc 7 appels de diffusion prévus si la transaction réussit, avec consommation de séquence et effets Realtime. Ne pas assimiler un UPDATE sans changement métier à une absence d'effets.

Les approbations JSON non NULL, les runs et les choix explicites sont conservés par les affectations directes. Le script ne vérifie pas que `set_approvals` contient un tableau; un JSON non conforme non NULL resterait non conforme. Il ne corrige pas non plus les valeurs invalides des CHECK : leur présence ferait échouer l'ajout des contraintes.

Les RPC recréées sont `SECURITY INVOKER`, avec `search_path=public`, mêmes noms d'arguments legacy `target_class_id`, et continuent à dépendre des droits de table/RLS. La source est horodatée via la RPC avec `auth.email()`; supprimer un éventuel trigger historique ferait perdre ce marquage pour les UPDATE directs. Ce trigger n'existe pas ici. Un argument NULL n'est pas rejeté par `NOT IN`, mais l'INSERT finit par rencontrer le NOT NULL. DROP/CREATE renouvelle l'identité de fonction et les grants par défaut; les anciens grants personnalisés ne sont pas conservés automatiquement. Ici l'accès PUBLIC/anon est retiré, authenticated est explicitement autorisé; les defaults postgres donnent aussi EXECUTE à service_role. Il faut contrôler l'ACL finale, sans supposer qu'authenticated est l'unique rôle autorisé.

**Conclusion d'idempotence :** les IF EXISTS/IF NOT EXISTS évitent certaines collisions mais ne rendent pas l'opération neutre. Chaque réexécution réécrit les setups et produit des effets annexes. L'état de schéma converge vers `scribe`, ce qui est précisément indésirable après la migration de defaults. Une colonne préexistante d'un type incorrect, une signature incompatible ou une dépendance ajoutée restent des causes d'échec.

**2. `20260807000400_repair_recurring_block_class_copy.sql`**

La première instruction classe **tous** les `class_templates` par `(created_at,id)` dans chaque `block_template_id`, attribue 1..N et ne modifie que les rangs différents. Elle ignore l'ordre métier existant : un ordre volontairement réorganisé est perdu. Le trigger de dates met à jour `updated_at` des modèles modifiés. Il n'existe pas de restriction à des modèles défectueux, une association ou une période.

La seconde instruction recherche les blocs créés à partir du **7 août 2026 à 20:13:45 UTC**, ayant au moins une classe liée à un modèle quelconque. Pour chaque modèle du bloc absent des classes, elle ajoute une classe à la fin, avec `max(sort_order)+row_number()`. Elle copie les valeurs actuelles du modèle : discipline, nom, code, niveau, frais, payout, politique de dossards, règles et notes. Elle impose `minimum_entries=2`, `registration_status='open'` et `is_public=true` indépendamment du statut du show/bloc. Les liens aux organismes et leurs métadonnées sont copiés uniquement pour les classes nouvellement insérées, avec `ON CONFLICT DO NOTHING` sur ces liens.

Le trigger réel `classes_copy_template_eligibility_requirements` ajoute aussi les exigences actives des modèles à `eligibility_requirements`. Cet effet n'apparaît pas dans le fichier de réparation. `set_class_refs` vérifie que la discipline est active et appartient à l'organisation du bloc, et que le modèle est compatible; une discipline devenue inactive ferait échouer l'insertion. Aucun insert d'inscription, de facture ou de paiement n'est demandé par ce chemin.

Limites importantes :

- Le cutoff provient d'un déploiement PROD selon le commentaire; il ne prouve pas qu'une absence de classe PREPROD est un bug.
- Les suppressions volontaires après le cutoff sont recréées; les modèles ajoutés après la création d'un bloc sont aussi copiés. Les montants viennent des modèles actuels, pas d'un état historique.
- Les blocs sans aucune classe liée à un modèle sont ignorés, même s'ils sont défectueux. Les blocs plus anciens restent ignorés. Une classe liée à un autre modèle peut suffire au test EXISTS.
- Le script ne répare ni les classes déjà copiées avec de mauvais frais/ordre ni leurs liens d'organismes manquants, et ne déduplique pas les classes déjà en double.
- Les classes insérées sont ouvertes et marquées publiques; leur visibilité effective dépend ensuite des autres règles de publication/RLS.
- Il n'y a pas de verrou applicatif pour `max+row_number` et `NOT EXISTS`. Une écriture concurrente peut provoquer un conflit sur `(block_id,sort_order)` ou un doublon logique de modèle. La contrainte unique sur la paire `(block_id,class_template_id)` n'existe pas dans le schéma observé. La protection ON CONFLICT concerne uniquement les organismes.

**PREPROD observée : 0 class_template, 0 candidat, 0 modèle à réordonner, 0 bloc du cutoff exclu faute de classe copiée, 0 doublon de modèle.** Les 8 blocs, 7 classes, 5 liens d'organismes, 7 exigences et 50 inscriptions ne seraient pas modifiés par cette migration. Factures et paiements : 0. L'état vide des modèles est une preuve datée d'absence d'impact, pas une garantie pour une exécution future.

**Conclusion d'idempotence :** après succès complet, sans changement des entrées ni concurrence, un second passage trouve les rangs corrects et aucun modèle manquant. Ce n'est pas une garantie de sécurité métier : une suppression volontaire ultérieure serait annulée par un nouveau passage. Aucune correction persistante du défaut des futurs modèles n'est installée par ce SQL; le flux applicatif de copie doit également être correct.

**3. `20260824130000_showscore_announcer_paid_warmup_live.sql`**

Ajoute une policy SELECT pour les annonceurs d'association et une RPC `SECURITY DEFINER` autorisant platform admin, admin/secrétaire/annonceur d'association, ou organizer/secrétaire/annonceur du show. Les helpers distants utilisent EXISTS et renvoient des booléens; l'autorisation interne est nécessaire parce que le propriétaire de la fonction contourne normalement RLS. Les grants de table existent et RLS est actif. La policy SELECT porte sur la ligne complète, sans limitation aux colonnes live. Les policies permissives existantes restent en place.

La RPC verrouille d'abord le warmup par `FOR UPDATE`, puis vérifie l'autorisation. Le verrou sérialise les mises à jour de la même ligne, mais ne détecte pas un client obsolète : avec la même identité de file, le dernier appel peut écraser les statuts précédents. La distinction entre warmup absent et accès interdit divulgue son existence et un appel non autorisé peut attendre/prendre brièvement le verrou avant rejet.

Quand `target_update_queue=true`, la file doit être un tableau. La comparaison retire seulement `status` et `completedAt`, conserve l'ordre et tous les autres champs : elle interdit ajout, suppression, réordonnancement ou changement d'identité. Les statuts autorisés sont pending/done/no_show/scratch. Un statut absent/NULL est traité comme pending pour la validation; `completedAt` n'est pas validé comme date. Ces restrictions s'appliquent aussi aux admins utilisant cette RPC. Les modifications sont limitées à la visibilité live, l'élément actif, son démarrage et la file.

Le trigger distant accepte les identifiants de la file ainsi que les identifiants synthétiques `drag-after-*` et `completed:drag-after-*`, et vérifie aussi les inscriptions HSP du bloc. Il n'impose pas une cohérence complète entre statut, élément actif et date. Si `target_update_queue` est faux/NULL, la file et l'élément actif sont conservés; un appel sans modification métier peut quand même toucher les dates et déclencher le broadcast. La fonction renvoie la ligne complète.

Aucune mutation du warmup n'a lieu lors de la création de la RPC. PREPROD contient **1 warmup**, file tableau composée d'objets, aucun élément actif. Son `id=block_id` implique que les futurs appels déclenchent aussi la synchronisation du bloc (dont libellé et `updated_at`) via `set_show_score_paid_warmup_refs`. Le chemin peut donc écrire au-delà des quatre colonnes live; c'est un effet du trigger préexistant.

**Conclusion d'idempotence :** DROP POLICY IF EXISTS + CREATE et CREATE OR REPLACE sont rejouables avec une signature compatible; aucun backfill. CREATE OR REPLACE ne supprime pas les ACL personnalisées : REVOKE ne vise que PUBLIC/anon. Un annonceur ayant par ailleurs un rôle plus privilégié peut toujours écrire directement via une policy existante; le commentaire « jamais UPDATE direct » n'est vrai que pour un annonceur sans autre droit.

**4. `20260824131000_showscore_secretary_schedule_writes.sql`**

`organizations.is_test_mode` est **absente** en PREPROD. L'ajout fournit false aux **4 organisations existantes**, ainsi qu'un DEFAULT false et NOT NULL pour les suivantes. Ce champ ne transforme pas l'environnement en mode test : PREPROD reste isolée par sa configuration, et les organisations ne deviennent pas automatiquement des organisations de test. Si une colonne homonyme existait avec type/default/nullabilité différents, IF NOT EXISTS ne les réconcilierait pas.

Les six policies permettent INSERT/UPDATE sur shows, show_days et blocks aux secrétaires de l'association. USING contrôle l'ancienne ligne; WITH CHECK contrôle la nouvelle. Les droits SELECT nécessaires existent déjà pour les membres, et les grants de table sont présents. Les policies ne limitent pas les colonnes aux champs d'horaire : elles ouvrent les autres colonnes modifiables, notamment statuts/visibilités. Une personne secrétaire de deux associations satisfait le prédicat des deux côtés d'un changement d'organisation; les relations et triggers doivent encore l'accepter. Ce n'est pas une règle générale d'immuabilité de l'organisation.

Les triggers de show_days et blocks contrôlent le contexte parent. Les modifications des dates d'un show peuvent créer automatiquement des journées via `ensure_show_days_from_show_dates`; les blocs déclenchent aussi les validations de dépendances d'horaire et les synchronisations existantes. Aucune ligne de calendrier n'est modifiée par CREATE POLICY lui-même. Aucune policy DELETE n'est ajoutée, mais les anciens droits ALL/DELETE restent applicables.

Le REVOKE/GRANT cible une RPC existante de signature exacte, trouvée en PREPROD. **Faille préexistante confirmée par son corps et ses ACL** : `create_association_with_owner` est SECURITY DEFINER, accepte un `target_id` arbitraire, fait ON CONFLICT(id) DO UPDATE sans contrôle de gestion, puis ajoute l'appelant comme admin s'il possède un profil et n'a pas déjà de membership. Un utilisateur authentifié non membre peut donc fournir l'UUID d'une organisation existante et emprunter ce chemin. Aucun essai d'exploitation n'a été exécuté. Aujourd'hui anon et authenticated ont EXECUTE; la migration retire anon/PUBLIC mais conserve authenticated. Elle réduit l'exposition anonyme sans corriger l'autorisation métier. Le search_path n'est pas fixé dans cette ancienne fonction, contrairement à la nouvelle RPC warmup.

**Conclusion d'idempotence :** stable pour la colonne absente puis correctement définie, la signature existante et les policies nommées. Cela ne prouve pas la sécurité de la RPC ni la limitation des UPDATE au calendrier.

Un autre constat d'ACL, préexistant et non introduit par ces migrations : les cinq tables inspectées pour les droits possèdent aussi des grants TRUNCATE à anon/authenticated. RLS ne filtre pas TRUNCATE. Ce n'est pas une preuve d'un endpoint REST capable de l'exécuter, mais les accès SQL/RPC exposant de telles opérations doivent être revus; ne pas présenter RLS comme une protection de ces privilèges. Les rôles anon/authenticated n'ont pas CREATE sur public dans le snapshot. Voir la [documentation PostgreSQL sur RLS](https://www.postgresql.org/docs/17/ddl-rowsecurity.html).

**Plan proposé, non exécuté**

1. Conserver ces preuves, figer le commit et la version effective du CLI utilisé pour le dry-run. Identifier les écritures HSP, ShowScore, robots, imports et jobs à suspendre pour la fenêtre PREPROD. Le préflight n'est pas une sauvegarde. Préparer une sauvegarde du schéma, des ACL et des données, et vérifier sa restauration dans un environnement isolé avant application.
2. Résoudre la régression du défaut avant d'autoriser le lot. Proposition : conserver les quatre fichiers historiques et préparer une nouvelle migration corrective versionnée qui réaffirme le contrat final `announcer` et `qualified_rider_count=6`. Elle doit faire partie du lot approuvé et s'exécuter avant réouverture des applications. Le nouveau dry-run annoncera alors plus de quatre migrations : faire approuver explicitement ce périmètre révisé. Ne pas modifier silencieusement une migration historique dont l'usage dans les autres environnements n'a pas été vérifié. Une révision de `02350` elle-même n'est envisageable qu'avec preuve qu'elle n'a été appliquée nulle part et validation de la chaîne canonique.
3. Préparer une correction séparée de `create_association_with_owner` : refuser un target_id existant dans le chemin création, ou contrôler explicitement les droits avant toute mise à jour; exiger un utilisateur/profil valide et fixer le search_path. Tester l'impossibilité d'obtenir admin sur une association tierce. Garder la réduction des ACL anonymes. Le choix du périmètre de colonnes accordé aux secrétaires doit être explicite.
4. Pour `07000400`, refaire le comptage juste avant la fenêtre. Si les modèles/candidats restent à zéro, approuver ce constat comme condition d'application. S'ils apparaissent, arrêter : produire la liste des IDs, ordres avant/après, montants et exigences copiés, et obtenir la justification métier de chaque réparation. Le cutoff seul n'est pas une preuve. Prévoir une réparation bornée aux cas démontrés, sans recréer les suppressions volontaires.
5. Dans une répétition isolée, reproduire **l'état PREPROD actuel avec les migrations ultérieures déjà appliquées**, puis appliquer le lot proposé. Un reset chronologique seul masquerait la régression `scribe`. Vérifier aussi une reconstruction chronologique et comparer leurs schémas finaux. Tester un second passage des SQL uniquement dans cette copie pour mesurer l'idempotence et les effets des triggers.
6. Exécuter sur la copie les tests `showscore_announcer_paid_warmup_live.sql` et `showscore_secretary_schedule_writes.sql`, puis compléter les cas négatifs : utilisateur tiers, anon, annonceur seul UPDATE direct refusé, ajout/suppression/réordre de file refusés, élément actif invalide, appels concurrents, création d'association avec UUID existant, secrétaire d'une autre association et champs hors calendrier. Les tests fournis incluent seed et écritures malgré leur rollback final : ne pas les lancer sur PREPROD dans le cadre de cet audit. Le test secrétaire retire des show_roles alors qu'un rôle authenticated est déjà actif; contrôler que le scénario obtenu est réellement « secrétaire d'association sans rôle show ». Tester la seconde migration avec rangs volontaires, classe supprimée, bloc vide, discipline inactive et exigences de modèle.
7. Après résultats validés et **autorisation distincte d'appliquer**, suspendre les écritures, reprendre une sauvegarde immédiate et rejouer le préflight ciblé. Arrêter à tout écart d'historique, de hash, de default, d'ACL ou de nombre attendu. Utiliser le mécanisme normal de migrations, ciblé explicitement sur PREPROD, sans SQL hors historique. Vérifier les frontières transactionnelles du CLI effectif dans la répétition; ne pas supposer une transaction atomique couvrant tout le lot. Fixer des délais de verrou/statement appropriés et arrêter à la première erreur. Une insertion tardive de versions anciennes nécessite le mode prévu par le CLI et un dry-run exact; elle ne justifie aucun repair.
8. Avant réouverture, contrôler : historique désormais conforme au lot autorisé; default final announcer et nombre qualifié 6; mêmes 7 setups et valeurs métier/approbations; seules dates et notifications prévues pour `02350`; mêmes 6 sessions et révisions; mêmes 8 blocs, 7 classes, 5 organismes, 7 exigences, 50 inscriptions tant que candidats=0; 0 facture/paiement; 4 organisations avec le nouveau champ attendu; warmup/file inchangés au déploiement. Vérifier les policies, ACL effectives, API PostgREST et écrans HSP/ShowScore; les tests fonctionnels ultérieurs produisent leurs propres écritures, à comptabiliser séparément.
9. En cas d'échec, conserver les logs, arrêter les écritures et comparer catalogue/historique réel avant reprise. Un lot partiellement validé ne se remet pas à zéro en supposant l'échec total. Revenir par migration compensatrice documentée ou restauration vérifiée selon le dommage, avec une nouvelle autorisation. Un retour arrière applicatif seul ne restaure ni défauts, ni ACL, ni données. Les séquences de broadcast peuvent avoir des trous malgré un rollback; un événement déjà livré après commit ne peut pas être « annulé ».

**Aucun `migration repair` proposé.** Les quatre versions manquantes sont cohérentes avec les écarts observés : colonne/policies/RPC manquantes et ACL anciennes. La présence de colonnes issues de `02300`, ou l'absence actuelle de modèles à réparer, ne prouve pas l'exécution de `02350`/`07000400`. Pour envisager ultérieurement un repair, il faudrait une preuve positive d'exécution du fichier exact (journal, hash, résultat transactionnel) et une réconciliation de tous ses effets avec l'historique, puis une autorisation explicite. Un dry-run vert ne fournit pas cette preuve.

Les principaux ALTER TABLE peuvent prendre un verrou exclusif même avec peu de données; la taille actuelle réduit le volume de travail sans garantir l'absence d'attente. Les CHECK/NOT NULL et les changements de type restent soumis aux dépendances et validations réelles. Voir [ALTER TABLE PostgreSQL 17](https://www.postgresql.org/docs/17/sql-altertable.html). DROP/CREATE et CREATE OR REPLACE n'ont pas les mêmes effets sur l'identité et les permissions des fonctions; voir [CREATE FUNCTION PostgreSQL 17](https://www.postgresql.org/docs/17/sql-createfunction.html).
